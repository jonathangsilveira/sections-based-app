package features

internal object Search {
    private const val prefix = ":app:feature:search:search"
    const val navigation = "$prefix-navigation"
    const val presentation = "$prefix-presentation"
}